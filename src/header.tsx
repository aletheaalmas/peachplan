export function Header() {
  return (
    <header>
      <div>
        <input type="text" placeholder="Search your projects" />
      </div>

      <button>+ Add Project</button>
    </header>
  );
}
